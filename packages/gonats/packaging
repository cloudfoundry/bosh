set -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang)
export GOPATH=$PWD
export PATH=$GOROOT/bin:$PATH

mkdir $BOSH_INSTALL_TARGET/bin

mv go/src ./src

# Compile go-nats
go build -o $BOSH_INSTALL_TARGET/bin/gnatsd github.com/cloudfoundry/gnatsd
