* Deployment failure points (from integration tests):
 - no networks specified (Array, empty is fine in the beginning but fails after network is being referenced from "compilation");
 - "compilation" not specified (Hash)
 - compilation["workers"] not specified (Integer)
 - compilation["network"] should reference existing network
 - each network has "name" (String) and "subnets" (Array)

Subnet:
 - network, range, gateway, dns, cloud_properties, netmask


 - compilation["cloud_properties"] not specified
 - "update" is missing (Hash)
 - update["canaries"] is missing (Integer, can be optional, depends on default_update_config param)
 - update["canary_watch_time"] ditto
 - update["max_in_flight"] ditto
 - update["update_watch_time"] ditto

- resource_pools (Array)

-- now manifest is (kinda) valid, check if release exists

Adding jobs:

- "name" (String)
- "template" (String)
- "resource_pool" (String, references existing resource pool)
- instances (Integer)
- networks (Array, name, static_ips(optional, if present then size should match instances number))


Resource pool:
- name (String)
- size (Integer, not explicitly checked though)
- cloud_properties (Hash)
- stemcell (Hash, name, version, network)

