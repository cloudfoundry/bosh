---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: bosh/os-image-stemcell-builder

inputs:
  - name: bosh-src
  - name: ubuntu-trusty-tarball
  - name: centos-7-tarball

outputs:
  - name: bosh-src-push

run:
  path: bosh-src/ci/pipelines/os-images/tasks/bump-os-image.sh
