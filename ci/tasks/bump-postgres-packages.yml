---
platform: linux

inputs:
- name: bosh-ci
- name: bosh
- name: postgres-src

outputs:
- name: bosh

run:
  path: bosh-ci/ci/tasks/bump-postgres-packages.sh

params:
  PRIVATE_YML:
  MAJOR_VERSION:
  GIT_USER_NAME:
  GIT_USER_EMAIL:

