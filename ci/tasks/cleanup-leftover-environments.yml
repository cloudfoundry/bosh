---
platform: linux
image_resource:
  type: docker-image
  source: {repository: bosh/integration}

inputs:
  - name: bosh-ci

run:
  path: bosh-ci/ci/tasks/cleanup-leftover-environments.sh

params:
  GCP_SERVICE_ACCOUNT_JSON:
  LEFTOVERS_PREFIX:
