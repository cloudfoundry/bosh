---
name: Test Director
port: 8081

logging:
  level: ERROR

redis:
  host: 127.0.0.1
  port: 63790
  password:

dir: /tmp/boshdir
db:
  database: "sqlite:///:memory:"
  max_connections: 32
  pool_timeout: 10
blobstore:
  plugin: simple
  properties:
    endpoint: http://127.0.0.1
    user: admin
    password:
compiled_package_cache:
  bucket: compiled_packages
  properties:
    provider: AWS
    aws_access_key_id: access key id
    aws_secret_access_key: secret access key

cloud:
  plugin: dummy
  # to be added
