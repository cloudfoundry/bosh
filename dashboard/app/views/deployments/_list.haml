%table(cellpadding=0 cellspacing=0)
  %tr
    %th.name Name
    
  - @deployments.each do |deployment|
    %tr{:class => cycle("odd", "even")}
      %td= h deployment.name

  %tr
    %td.total= "Total: %s" % [ @deployments.size ]
