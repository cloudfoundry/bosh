{
       defaults {
          user_friendly_names no
       }
       blacklist {
         wwid "SAdaptec*"
         devnode "^hd[a-z]"
         devnode "^(ram|raw|loop|fd|md|sr|scd|st|xvda|xvdb|xvdc)[0-9]*"
         devnode "^cciss.*"
       }
}