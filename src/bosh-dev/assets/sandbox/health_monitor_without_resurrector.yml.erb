---
http:
  port: <%= hm_port %>

mbus:
  endpoint: nats://localhost:<%= nats_port %>
  user:
  password:

director: &director
  endpoint: <%= director_url %>
  user: admin
  password: admin

intervals:
  poll_director: 5
  poll_grace_period: 5
  log_stats: 10
  analyze_agents: 10
  agent_timeout: 10
  rogue_agent_alert: 20

plugins:
  - name: logger
    events:
      - alert
  - name: nats
    events:
      - alert
      - heartbeat
    options:
      endpoint: nats://localhost:<%= nats_port %>
      user:
      password:
