#!/bin/sh
set -e


echo "Extracting PowerDNS ..."
tar xzvf pdns-src/pdns-3.2.tar.gz

if [[ $? != 0 ]] ; then
  echo "Failed extracting powerdns"
  exit 1
fi

echo "Building cdrtools ..."
cd pdns-3.2
./configure --prefix=${BOSH_INSTALL_TARGET}
make
make install